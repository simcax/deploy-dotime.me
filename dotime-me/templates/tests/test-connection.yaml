apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dotime-me.fullname" . }}-test-connection"
  labels:
    {{- include "dotime-me.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dotime-me.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
