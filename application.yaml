apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: skov-run
  namespace: argocd
spec:
  project: default

  source: 
    repoURL: https://github.com/simcax/deploy-dotime.me.git
    targetRevision: HEAD
    path: dotime-me
  destination:
    server: https://kubernetes.default.svc
    namespace: do-time-dev
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
